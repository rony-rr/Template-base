import * as React from 'react';
import { 
    StyleSheet
} from 'react-native';
import Constants from 'expo-constants';

import GlobalVars from '../../global/globalVars';

export const Styles = StyleSheet.create({
    
    

});

export const MyTheme = {
    dark: false,
    colors: {
      card: GlobalVars.grisOscuro,
      border: 'transparent',
    },
};